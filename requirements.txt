# Minimal Production Requirements - Under 250MB for Vercel
# API calls work via requests, no heavy SDKs needed

# Core Image Processing (REQUIRED) ~30MB
Pillow==10.1.0

# Numerical operations for color analysis ~15MB
numpy==1.26.0

# HTTP Client for all API calls ~5MB
requests==2.31.0

# Environment variables ~1MB
python-dotenv==1.0.0

# AI SDK for Pantone color matching ~10MB total
anthropic==0.66.0
httpx  # Required by anthropic

# Google AI SDK for Gemini (REQUIRED) ~15MB
google-genai

# TOTAL: ~75MB (well under 250MB limit)
# Both Anthropic and Google GenAI SDKs included
# Full AI-powered functionality for production