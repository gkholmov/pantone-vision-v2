# Minimal Production Requirements - Under 250MB for Vercel
# API calls work via requests, no heavy SDKs needed

# Core Image Processing (REQUIRED) ~30MB
Pillow==10.1.0

# Numerical operations for color analysis ~15MB
numpy==1.26.0

# HTTP Client for all API calls ~5MB
requests==2.31.0

# Environment variables ~1MB
python-dotenv==1.0.0

# AI SDK for Pantone color matching ~2MB
anthropic==0.39.0

# Google AI SDK for Gemini (optional, will use fallback if not available)
# google-generativeai==0.8.3

# TOTAL: ~53MB (well under 250MB limit)
# All API functionality works via HTTP requests
# Includes full Pantone AI matching capabilities