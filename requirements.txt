# Minimal requirements for Vercel Python serverless functions
# Only essential packages that are actually imported and used

# Image Processing (required for all endpoints)
Pillow==10.1.0

# Basic HTTP requests (for API calls)
requests==2.31.0

# Environment variables
python-dotenv==1.0.0